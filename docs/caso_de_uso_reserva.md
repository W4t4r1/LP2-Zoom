# Especificación de Caso de Uso: CU-02 (LP2-Zoom)

Este documento detalla la especificación técnica del caso de uso **Admitir / Rechazar Invitados** (Moderación en Sala de Espera) extraído formalmente del Diagrama de Casos de Uso, el Diagrama de Secuencia y la sección 6.3 del reporte principal del proyecto **[informe.md](file:///c:/Users/lorox/OneDrive/Desktop/LP2-Zoom/LP2-Zoom/docs/informe.md)**.

---

## Ficha del Caso de Uso: Admitir / Rechazar Invitados

| Campo | Detalle |
| :--- | :--- |
| **Código** | **CU-02** |
| **Nombre** | **Admitir / Rechazar Invitados** |
| **Actor principal** | **Anfitrión (Host / Docente)** (Usuario que administra la videoconferencia). |
| **Actores secundarios** | **Invitado (Guest / Estudiante)** (Usuario en sala de espera), **Servidor (ManejadorCliente)**, **Base de Datos (Supabase Cloud PostgreSQL)**. |
| **Objetivo** | Permitir al Anfitrión moderar el acceso de los Invitados que se encuentran en la sala de espera en tiempo real, aprobando o denegando su ingreso a la reunión. |
| **Precondición** | 1. El Anfitrión debe haber creado una sala activa en el sistema y estar en la pantalla de espera del Host (`HOST_WAITING`).<br>2. Uno o más Invitados deben haber enviado una solicitud de unión (`JOIN_ROOM_REQUEST`) con el código de 6 caracteres y encontrarse registrados en estado `"PENDIENTE"` en la tabla `SolicitudesSala` de Supabase. |
| **Postcondición** | 1. El registro del Invitado en la tabla `SolicitudesSala` cambia de `"PENDIENTE"` a `"ACCEPTED"` (Aceptado) o `"RECHAZADO"` (Rechazado).<br>2. Si es admitido, se inserta una fila en `ParticipantesSala` con estado `"ACTIVO"` y el Invitado transiciona al estado `"INVITADO_ADMITIDO"`, esperando la orden de inicio oficial.<br>3. Si es rechazado, se le notifica por red y regresa al selector principal (`SELECTOR`).<br>4. Se actualiza el listado gráfico de solicitudes en la UI del Host. |
| **Flujo principal** | **1.** El Invitado solicita unirse enviando la trama `JOIN_ROOM_REQUEST`. El Servidor registra la solicitud en Supabase como `"PENDIENTE"` y bloquea la interfaz del Invitado.<br>**2.** El Servidor recupera las solicitudes pendientes de la sala en Supabase y las envía al Host en la trama `WAITING_ROOM_UPDATE`.<br>**3.** El cliente del Anfitrión recibe la trama y actualiza de forma segura la lista gráfica `JList` con los nombres de los candidatos en espera (dentro del Event Dispatch Thread mediante `SwingUtilities.invokeLater`).<br>**4.** El Anfitrión selecciona al Invitado de la lista en espera y hace clic en el botón "Admitir".<br>**5.** El cliente del Anfitrión envía una trama JSON de tipo `ADMIT_USER` con el mensaje `"ACEPTAR"` y el ID del Invitado seleccionado por el socket TCP.<br>**6.** El hilo `ManejadorCliente` en el servidor recibe la trama, interpreta el comando y, mediante `DBProxy`, actualiza la solicitud en la tabla `SolicitudesSala` a estado `"ACCEPTED"`.<br>**7.** El Servidor inserta al Invitado en la tabla `ParticipantesSala` con estado `"ACTIVO"` y con la fecha y hora de ingreso.<br>**8.** El Servidor envía al cliente del Invitado una trama JSON de tipo `ADMIT_USER` con el mensaje `"ACCEPTED"`.<br>**9.** El cliente del Invitado procesa la trama y actualiza su estado visual en pantalla a "Invitado Admitido" (Espera de inicio oficial de la reunión por parte del Host). |
| **Flujo alternativo** | **4a. El Anfitrión rechaza la solicitud:**<br>&nbsp;&nbsp;&nbsp;&nbsp;1. En el paso 4, el Anfitrión selecciona al candidato en espera y hace clic en el botón "Rechazar".<br>&nbsp;&nbsp;&nbsp;&nbsp;2. El cliente envía la trama `ADMIT_USER` con el mensaje `"RECHAZAR"` y el ID del Invitado.<br>&nbsp;&nbsp;&nbsp;&nbsp;3. El Servidor recibe el comando, actualiza el registro en la tabla `SolicitudesSala` a estado `"RECHAZADO"` en Supabase y envía la trama `ADMIT_USER` con el mensaje `"REJECTED"` al socket del Invitado.<br>&nbsp;&nbsp;&nbsp;&nbsp;4. El cliente del Invitado recibe la trama de rechazo, sale de la pantalla de bloqueo y regresa al panel selector (`SELECTOR`) mostrando el aviso de que fue rechazado (Fin del flujo alternativo). |
| **Reglas de negocio** | **RN-01 (Aislamiento de la BD - Regla de Oro):** Toda modificación física de los estados de admisión en `SolicitudesSala` y `ParticipantesSala` se realiza de forma exclusiva en el Servidor a través del proxy `DBProxy`. El cliente jamás ejecuta sentencias JDBC directas.<br>**RN-02 (Concurrencia Segura en Swing):** Las actualizaciones asíncronas de la lista de espera recibidas en la trama `WAITING_ROOM_UPDATE` se gestionan por el hilo de red secundario (`HiloEscuchaCliente`) y se pintan en la interfaz gráfica delegando en el EDT mediante `SwingUtilities.invokeLater(...)`. |
| **Datos de entrada** | Trama JSON (`MensajeSocket`) enviada por el Host:<br>- `type`: `"ADMIT_USER"` (String)<br>- `roomCode`: Código alfanumérico de la sala (String)<br>- `userId`: ID numérico del Invitado a moderar (int)<br>- `message`: `"ACEPTAR"` o `"RECHAZAR"` (String) |
| **Datos de salida** | Trama JSON (`MensajeSocket`) enviada por el Servidor al Invitado:<br>- `type`: `"ADMIT_USER"` (String)<br>- `message`: `"ACCEPTED"` o `"REJECTED"` (String) |
| **Excepciones** | **EX-01: Desconexión abrupta de red de un Invitado en espera:**<br>&nbsp;&nbsp;&nbsp;&nbsp;1. Si el socket físico de un Invitado se cierra inesperadamente mientras está en espera, el Servidor captura la excepción de I/O.<br>&nbsp;&nbsp;&nbsp;&nbsp;2. El Servidor ejecuta el método de limpieza `desconectar()`, elimina la solicitud pendiente de `SolicitudesSala` y difunde una nueva trama `WAITING_ROOM_UPDATE` al Anfitrión para remover el candidato huérfano de la lista de la interfaz (Fin del caso de uso). |
